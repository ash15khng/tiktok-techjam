# The deterministic agent is the default. Enabling the optional parser requires
# both an API key and an explicit model available to your OpenAI project.
SHOPPING_COPILOT_LLM_ENABLED=0
SHOPPING_COPILOT_LLM_MODEL=
OPENAI_API_KEY=
